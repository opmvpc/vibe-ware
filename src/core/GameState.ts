export enum GameState {
  MENU,
  PLAYING,
  GAME_OVER,
  DEBUG_MENU,  // Menu de sélection des jeux en mode debug
  DEBUG_GAME,   // Jeu en cours d'exécution en mode debug
  PAUSED      // Nouvel état pour le menu pause
}
